[package]
name = "pingtest-workspace"
version = "1.1.0"
edition = "2021"

[workspace]
members = ["src-tauri"]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
